// Всегда можно узнать тип переменной используя команду typeof

typeof 'String';

typeof 123;

typeof { 'key': 'value' };

typeof ['value', 'value'];

var getSomething = function() {};

typeof getSomething;

var wallet = null;
